[phases.setup]
nixPkgs = ["python310", "postgresql"]

[phases.install]
cmds = [
  "cd backend",
  "pip install -r requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "cd backend && gunicorn run:app --workers=4 --bind=0.0.0.0:$PORT"
